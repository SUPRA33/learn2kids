import logo from "../assets/img/logo.png";

const Header = () => {
  return (
    <header>
      <img id="logo-header" src={logo} alt="logo learn to kids" />
    </header>
  );
};

export default Header;
